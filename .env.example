# Wird von install.sh automatisch erzeugt (Passwörter zufällig generiert).
# Nur relevant, falls docker-compose manuell statt über install.sh gestartet wird.
DOMAIN=cms.deine-domain.de
DB_PASSWORD=
DB_ROOT_PASSWORD=
